# Checklist

- [x] 后端 `GET /api/family/menu` 返回按天/餐次分组的本周菜单
- [x] 后端 `POST /api/family/menu` 可添加菜谱到指定日期和餐次
- [x] 后端 `POST /api/family/menu/{id}/vote` 可投票，返回更新后投票数
- [x] 后端 `GET /api/family/shopping` 返回按品类分组的购物清单
- [x] 后端 `POST /api/family/shopping` 可添加购物项
- [x] 后端 `PATCH /api/family/shopping/{id}` 可勾选/取消勾选
- [x] 后端 `DELETE /api/family/shopping/{id}` 可删除购物项
- [x] 前端 api.js 包含 beautifyImage、getRecommendations、菜单、购物清单方法
- [x] AI记录页识别后自动调用美化接口，展示原图/美化图对比
- [x] 美化失败时降级为原图，不阻塞保存流程
- [x] AI记录页展示营养卡片（热量/蛋白质/脂肪/碳水）
- [x] 保存记录时使用用户选择的图片版本URL
- [x] 首页"为你推荐"区域正常渲染推荐菜谱卡片
- [x] 推荐卡片展示菜名、难度、烹饪时间、匹配度
- [x] 家庭菜谱页支持"菜谱"/"本周菜单"Tab切换
- [x] 本周菜单视图展示周一至周日早午晚餐
- [x] 菜谱卡片有"加入菜单"按钮，可选择日期和餐次
- [x] 菜单项支持投票（赞/踩），显示投票计数
- [x] 购物清单按品类分组展示，支持勾选
- [x] 购物清单支持添加新物品
- [x] 购物清单支持删除物品
- [x] 新增功能在移动端和桌面端布局正常
- [x] 核心闭环验证：拍照→识别→美化→营养→保存→首页推荐→家庭菜单→购物清单
